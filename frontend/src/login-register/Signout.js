import React, { useState } from 'react';
import './Signout.css';

function Signout() {

    return (
        <div>
            <button>Sign Out</button>
        </div>
    );
}

export default Signout